name: Check Service Principal

on:
  workflow_dispatch:
  schedule:
    - cron: "35 6 * * *"

jobs:
  set-matrix:
    runs-on: ubuntu-latest
    outputs:
      tests: ${{ steps.select-tests.outputs.tests }}
    steps:
      - name: Select Tests
        id: select-tests
        run: |
          d="{'environment' :'Development'  , 'principal': 's105d01-keyvault-readonly-access'}"
          t="{'environment' :'Test'         , 'principal': 's105t01-keyvault-readonly-access'}"
          p="{'environment' :'Production'   , 'principal': 's105p01-keyvault-readonly-access'}"
          tests="{ 'data':[ ${d} ,  ${t} ,  ${p} ]}"
          echo "::set-output name=tests::${tests}"

  check_expires:
    name:      ${{matrix.data.environment}}/${{ matrix.data.principal }}
    needs:     set-matrix
    strategy:
       matrix: ${{ fromJson(needs.set-matrix.outputs.tests) }}
    environment:
         name: ${{ matrix.data.environment }}
    runs-on: ubuntu-latest
    steps:
      - name: Check ${{ matrix.data.principal }}
        uses: DFE-Digital/github-actions/CheckServicePrincipal@master
        id: pwsh_check_expire
        with:
            AzureCredentials: ${{ secrets.AZURE_CREDENTIALS }}
            ServicePrincipal: ${{ matrix.data.principal }}
            ExpiresWithinDays: 30

      - name: Processing ${{ matrix.data.principal }}
        if:   fromJson(steps.pwsh_check_expire.outputs.json_data).data.Alert == true
        run:  echo Expires in ${{fromJson(steps.pwsh_check_expire.outputs.json_data).data.ExpiresDays}} days

      - name: Check out the repo
        if:   fromJson(steps.pwsh_check_expire.outputs.json_data).data.Alert == true
        uses: actions/checkout@v3

      - name: set-up-environment
        if:   fromJson(steps.pwsh_check_expire.outputs.json_data).data.Alert == true
        uses: DFE-Digital/github-actions/set-up-environment@master

      - uses: Azure/login@v1
        if:   fromJson(steps.pwsh_check_expire.outputs.json_data).data.Alert == true
        with:
            creds: ${{ secrets.AZURE_CREDENTIALS }}

      - uses: DfE-Digital/keyvault-yaml-secret@v1
        if:   fromJson(steps.pwsh_check_expire.outputs.json_data).data.Alert == true
        id:  keyvault-yaml-secret
        with:
          keyvault: ${{ secrets.KEY_VAULT}}
          secret: SE-INFRA-SECRETS
          key: SLACK-WEBHOOK
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}

      - name: Slack Notification
        if:   fromJson(steps.pwsh_check_expire.outputs.json_data).data.Alert == true
        uses: rtCamp/action-slack-notify@master
        env:
           SLACK_COLOR: ${{env.SLACK_ERROR}}
           SLACK_TITLE: Service Principal ${{ fromJson(steps.pwsh_check_expire.outputs.json_data).data.Application }} secret ${{ fromJson(steps.pwsh_check_expire.outputs.json_data).data.Name }} expiring soon
           SLACK_MESSAGE: |
                         The Service Principal ${{ fromJson(steps.pwsh_check_expire.outputs.json_data).data.Application }}
                         secret ${{ fromJson(steps.pwsh_check_expire.outputs.json_data).data.Name }} is due to expire in ${{fromJson(steps.pwsh_check_expire.outputs.json_data).data.ExpiresDays}} days
                         Please follow the 'Use the service principal in external systems' process in https://dfe-technical-guidance.london.cloudapps.digital/infrastructure/hosting/azure-cip/#service-principal to renew.
           SLACK_WEBHOOK: ${{ steps.keyvault-yaml-secret.outputs.SLACK-WEBHOOK }}
